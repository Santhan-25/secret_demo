name: Secret and environment variables

on: [push, workflow_dispatch]

jobs:
  default-env-variables:
    runs-on: ubuntu-latest
    steps:
      - name: Run Secret variable
        run: |
          echo "$DEVOPS"
          if [${{ secrets.DEVOPS_SAN }} === "Woww Secret"}]; 
          then 
            echo "Got out Secret"
          fi
          then 
            echo "Got out Secret"
          fi
        env:
          DEVOPS: ${{ secrets.DEVOPS_SAN }}
